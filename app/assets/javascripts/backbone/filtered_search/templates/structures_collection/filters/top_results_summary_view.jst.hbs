{{#if subject_name}}
    Tous les cours de <strong>{{ subject_name }}</strong> à <strong class="text-transform-capitalize">{{ city }}</strong>
{{else}}
    Tous les cours à <strong class="text-transform-capitalize">{{ city }}</strong>
{{/if}}

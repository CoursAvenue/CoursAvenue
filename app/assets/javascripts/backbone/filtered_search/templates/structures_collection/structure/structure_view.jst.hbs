<a href="{{ data_url }}" class="muted-link grid--full f-weight-500 block bordered--bottom push-half--bottom soft-half--bottom">
    <div class="grid__item four-twelfths relative rslides-wrapper push-half--bottom" data-resizeable-height style="height: 200px">
        <ul class="rslides">
            {{#if preloaded_medias}}
                {{#each preloaded_medias}}
                    <li class="bg-cover" data-resizeable-height style="height: 200px">
                        <div class="media__item media__item--slides">
                            <span class="break-all" title="{{this.caption}}">
                                <img src="{{this.thumbnail_url}}" alt="{{this.caption}}"/>
                            </span>
                        </div>
                    </li>
                {{/each}}
            {{else}}
                <li class="bg-cover" data-resizeable-height style="height: 200px">
                    <div class="media__item media__item--slides">
                        <span class="break-all">
                            <img src="{{logo_large_url}}" itemprop="logo"/>
                        </span>
                    </div>
                </li>
            {{/if}}
        </ul>
        <div rel="slideshow-{{mediable_id}}" class="rslides_nav one-whole text--center">Voir le profil</div>
        <div href="#" class="orange north east absolute push-half on-under-top heart-link" data-behavior='add-to-favorite' data-toggle='tooltip' data-added-title='Retirer de mes favoris' data-not-added-title='Ajouter à mes favoris' data-placement='left'>
            <i class="fa fa-heart-o fa-3x"></i>
        </div>
    </div>{{!--
--}}<div class="grid__item eight-twelfths soft--left epsilon line-height-2">
        <h4 class="flush line-height-2">
            {{ truncate name 45 }}
        </h4>
        <div>
            <div>
                {{#if structure_type}}
                    <div>
                        {{structure_type}}
                    </div>
                {{/if}}
                <div>
                    {{#if highlighted_comment_title}}
                        <i class="inline-block v-middle text-ellipsis bordered--left soft-half--sides gray bordered--thick" style="max-width: 80%;">
                            {{{ highlighted_comment_title }}}
                        </i>・
                    {{/if}}
                    <div class="inline-block v-middle">
                        <i class="fa fa-comment"></i> {{ comments_count }} avis
                    </div>
                </div>
                <div class="text-ellipsis">
                    {{ subjects }}
                </div>
            </div>
{{!--             <div class="v-middle nowrap text--center soft-half--sides">
                {{#if is_open_for_trial}}
                    <div class="green f-weight-bold">Essai gratuit</div>
                {{/if}}
                {{#if is_open_for_trial}}{{#if has_promotion}}
                <div class="green f-weight-bold">&</div>
                {{/if}}{{/if}}
                {{#if has_promotion}}
                    <div class="green f-weight-bold">Promotions</div>
                {{/if}}
            </div>
 --}}
         </div>
    </div>
</a>

SearchPage.module('Router', function(Module, App, Backbone, Marionette, $, _) {
    Module.SearchPageRouter = Backbone.Router.extend({
        routes: {
        },

        // initialize: function initialize (options) {
        // }
    });
});

{{#each breadcrumbs}}
  <span class="lbl v-middle pointer" data-type="clear" data-target="{{target}}" {{#if title}} data-behavior="tooltip" title="{{ title }}"{{/if}}>
    <i class="fa fa-times soft-half--right"></i>{{name}}
  </span>
{{/each}}

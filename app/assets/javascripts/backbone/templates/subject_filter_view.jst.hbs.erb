<div class="grid--full">{{#each subjects}}{{!--
   --}}<div class="grid__item one-quarter very-soft">
      <div data-type="button" data-value="{{slug}}" class="text--center btn one-whole">
        <img class="center-block very-soft" src="<%= image_path("icons/subjects/{{slug}}.png") %>" height="35" />
        {{name}}
      </div>
  </div>{{!--
 --}}{{/each}}{{!--
 --}}<div class="grid__item one-quarter very-soft">
  <div data-type="button" data-value="other" class="text--center btn one-whole">
    <img class="center-block very-soft" src="assets/icons/subjects/other.png" height="35" />
    Autres
  </div>
</div>{{!--
 --}}</div>

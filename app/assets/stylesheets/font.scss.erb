@import "compass/css3/font-face";

@include font-face(
    'Gotham-Bold',
    font-files(
        font-path('gothambold/gothambold.woff'), woff,
        font-path('gothambold/gothambold.ttf'), truetype,
        font-path('gothambold/gothambold.svg') + '#GothamBold', svg),
        font-path('gothambold/gothambold.eot'),
        normal,
        normal);

@include font-face(
    'Gotham-Book',
    font-files(
        font-path('gothambook/gothambook.woff'), woff,
        font-path('gothambook/gothambook.ttf'), truetype,
        font-path('gothambook/gothambook.svg') + '#GothamBook', svg),
        font-path('gothambook/gothambook.eot'),
        normal,
        normal);

@include font-face(
    'Gotham-Book',
    font-files(
        font-path('gothambold/gothambold.woff'), woff,
        font-path('gothambold/gothambold.ttf'), truetype,
        font-path('gothambold/gothambold.svg') + '#GothamBold', svg),
        font-path('gothambold/gothambold.eot'),
        bold,
        bold);

@include font-face(
    'Gotham-Light',
    font-files(
        font-path('gothamlight/gothamlight.woff'), woff,
        font-path('gothamlight/gothamlight.ttf'), truetype,
        font-path('gothamlight/gothamlight.svg') + '#GothamLight', svg),
        font-path('gothamlight/gothamlight.eot'),
        normal,
        normal);

@include font-face(
    'Gotham-Medium',
    font-files(
        font-path('gothammedium/gothammedium.woff'), woff,
        font-path('gothammedium/gothammedium.ttf'), truetype,
        font-path('gothammedium/gothammedium.svg') + '#GothamMedium', svg),
        font-path('gothammedium/gothammedium.eot'),
        normal,
        normal);

<% if @plan.persisted? %>
    COURSAVENUE.helperMethods.flash("Offre bien créée.", 'success');
    $.fancybox.close()
    location.reload();
    // TODO: add sexyness
<% else %>
    COURSAVENUE.helperMethods.flash("Erreur lors de la création de l'offre, veuillez rééssayer.", 'error');
<% end %>

- if @structure.logo?
  - content_for :logo do
    = link_to structure_url(@structure, subdomain: 'www'), target: '_blank' do
      = image_tag @structure.logo.url(:thumb), style: '-ms-interpolation-mode:bicubic;', width: '75', height: '75'

- content_for :promo do
  %h4
    #{@structure.name} vous invite à mettre en avant la qualité de vos cours sur CoursAvenue.com.

- if @email_text
  = @email_text.html_safe

%p.action
  = link_to 'Soyez recommandé par vos élèves', inscription_pro_structures_url(email: @email, from_friend: true, subdomain: 'pro'), class: 'btn btn--blue'
%p
  À très bientôt !
%p= @structure.name

- content_for :footer do
  %p Vous recevez cet email car #{@structure.name} vous invite sur CoursAvenue.

= content_for :header do
  = render 'shared/headers/pro_pages'

= content_for :content do
  = yield

  = render 'shared/footer'

= render :template => 'layouts/application'
= render 'shared/live_chat'

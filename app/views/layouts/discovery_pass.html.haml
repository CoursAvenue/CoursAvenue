= content_for :header do
  %header.header.print--hidden
    .very-soft.soft-half--sides.flexbox.main-container.main-container--medium
      .flexbox__item.palm-one-whole.v-middle>
        = link_to '', root_url(subdomain: 'www'), class: 'v-middle coursavenue-header-logo mixpanel-tracker', title: 'CoursAvenue | Tous les cours autour de vous', data: { info: 'header' }

= content_for :content do
  .main-container
    = yield

  = render 'shared/footer'

= render :template => 'layouts/application'

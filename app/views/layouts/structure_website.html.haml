- if @structure.medias.any?
  - image = @structure.medias.cover_first.images_first.first.image
  #photos-videos.relative.bg-position-top.bg-cover.one-whole.z-index-under{ style: "background-image: url('#{image.url(:wide_and_blurry)}')", data: { behavior: 'parallax' } }
    .black-curtain.black-curtain--soft.north.west.one-whole.absolute
    .soft
      %h1.text--center.white.relative.push--ends
        = @structure.name

%ul.text--center.push.nav
  = link_to 'Présentation', structure_website_presentation_path, class: ['delta soft-half--sides', (action_name == 'index' ? 'f-weight-bold' : '')]
  = link_to 'Planning', structure_website_planning_path, class: ['delta soft-half--sides', (action_name == 'planning' ? 'f-weight-bold' : '')]
  = link_to "Livre d'Or", structure_website_reviews_path, class: ['delta soft-half--sides', (action_name == 'reviews' ? 'f-weight-bold' : '')]
  = link_to 'Photos / Vidéos', structure_website_medias_path, class: ['delta soft-half--sides', (action_name == 'medias' ? 'f-weight-bold' : '')]
  = link_to 'Contact', structure_website_contact_path, class: ['delta soft-half--sides', (action_name == 'contact' ? 'f-weight-bold' : '')]

= content_for :content do
  .main-container.main-container--medium{ class: controller_path }
    = yield

= render :template => 'layouts/application'

#coursavenue-footer

- content_for :title do
  = @newsletter.title

- content_for :email_title do
  = @newsletter.title

- content_for :permalink do
  Si vous ne parvenez pas à lire cet email,
  = link_to "cliquez ici", structure_newsletter_url(@newsletter.structure, @newsletter), target: :_blank

%table.promo-table{ border: '0', cellpadding: '0', cellspacing: '0', width: '600' }
  %tr
    %td.content-block
      .text--center
        %h2= @newsletter.title

= render partial: "newsletter_mailer/newsletter_layouts/#{@newsletter.layout.partial}", locals: { blocs: @blocs }

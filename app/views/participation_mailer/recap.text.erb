Connexion : <%= new_user_session_url(subdomain: 'www') %>
JPO : <%= open_courses_url(subdomain: 'www') %>
Rechercher : <%= structures_url(subdomain: 'www') %>

<% if @participations.length == 1 %>
Récapitulatif de votre inscription aux Portes Ouvertes CoursAvenue
<% else %>
Récapitulatif de vos inscriptions aux Portes Ouvertes CoursAvenue
<% end %>

Nous approchons de l'événement à grand pas. Pour ne passer à côté de rien, imprimez toutes les informations des ateliers auxquels vous participez depuis votre profil :

Imprimer les informations : <%= user_participations_url(@user) %>

<% if @participations.length == 1 %>
Ci-dessous, un récapitulatif de votre inscription :
<% else %>
Ci-dessous, un récapitulatif de vos inscriptions :
<% end %>

<% @participations.each_with_index do |participation, index| %>
<% course = participation.course %>
<% place  = participation.planning.place %>

<%= course.name %> — par <%= course.structure.name %>
<%= l(participation.planning.start_date, format: :semi_long).capitalize %>
<%= "de #{l(participation.planning.start_time, format: :short)} à #{l(participation.planning.end_time, format: :short)}" %>

<%= place.location.name %> <%= place.location.address %>
Voir l'adresse sur Google Maps : http://maps.google.com/?q=<%=place.location.address %>
  <% if course.info.present? %>
    Infos pratiques sur le cours :
    <%= course.info %>
  <% end %>
  <% if place.info.present? or place.private_info.present? %>
    Infos pratiques sur le lieu : <%= place.info %> <%= place.private_info %>
  <% end %>
<% end %>

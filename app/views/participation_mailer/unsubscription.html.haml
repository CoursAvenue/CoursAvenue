- content_for :header_links do
    = link_to 'Connexion', new_user_session_url(subdomain: 'www')
    \|
    = link_to 'JPO', open_courses_url(subdomain: 'www')
    \|
    = link_to 'Rechercher', structures_url(subdomain: 'www')

- content_for :email_logo do
  = image_tag 'jpo_2014/logo_1_lines_orange.png', alt: 'CoursAvenue', height: '125', width: '225', style: '-ms-interpolation-mode:bicubic;'

- if @structure.logo.present?
  - content_for :logo do
    = image_tag @structure.logo.url(:thumb), alt: 'CoursAvenue', width: '100', height: '100', style: '-ms-interpolation-mode:bicubic;'

- content_for :promo do
  %h4
    Votre inscription a bien été annulée

%p
  Vous venez d'effectuer une demande de désinscription qui a bien été prise en compte. Retrouvez toutes les informations directement sur votre profil :

%p.action
  = link_to "Voir mon profil", user_participations_url(@user), class: 'btn btn--green'

%p.blue.blue-tip
  %i Trucs & astuces : depuis votre profil, invitez vos amis, familles et collègues à Participer aux Portes Ouvertes avec vous.

- content_for :under_content do
  = render 'user_mailer/footer_info'


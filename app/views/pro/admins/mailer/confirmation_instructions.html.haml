- content_for :email_title do
  - if @resource.sign_in_count > 0
    Nous avons bien pris en compte votre changement d'e-mail.
  - else
    Bienvenue sur CoursAvenue ! Pour commencer, vous devez confirmer votre adresse e-mail.

- @admin      = @resource
- @structure  = @admin.structure
- @structures = @structure.similar_profiles(4)


- if @resource.sign_in_count > 0
  .p Bonjour
  .p
    Nous avons bien pris en compte votre changement d'e-mail. Pour valider ce changement veuillez cliquer sur le lien de validation suivant :
  .p.action
    = link_to pro_admin_confirmation_url(confirmation_token: @token, subdomain: CoursAvenue::Application::PRO_SUBDOMAIN), class: 'btn btn--green' do
      Valider mon nouvel e-mail
- else
  .title
    Bienvenue sur CoursAvenue !

  .p Accédez dès maintenant à votre profil pour valider votre inscription et votre e-mail :

  .p.action
    = link_to pro_admin_confirmation_url(confirmation_token: @token, subdomain: CoursAvenue::Application::PRO_SUBDOMAIN), class: 'btn btn--green' do
      Valider et compléter mon profil

  = content_for :under_content do
    %table{ align: 'center', border: '0', cellpadding: '0', cellspacing: '0', width: '100%' }
      %tr{ valign: 'top' }
        %td.content-block{ colspan: 2 }
          .title Votre profil complet en quelques minutes :
      %tr{ valign: 'top' }
        %td.text--center{ width: '100' }
          = image_tag @structure.logo.url(:thumb), alt: "Logo #{@structure.name}", height: '100', width: '100'
        %td.content-block.soft--left
          %ul
            %li Compléter mes informations générales et ajouter un logo
            %li Demander des recommandations à mes élèves
            %li Renseigner mes cours et mon planning
            %li Mettre en ligne mes photos et vidéos

  = render 'shared/mailers/footer'


  - if @structures.length == 4
    - content_for :under_content_2 do
      = render 'admin_mailer/similare_profiles'

  - content_for :under_content_gray do
    = render 'admin_mailer/visite_100k_visits'

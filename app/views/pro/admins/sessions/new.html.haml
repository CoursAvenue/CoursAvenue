.page
  %h2.text--center= t('.log_in')

  = simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
    .form-inputs
      = f.input :email, :required => false, :autofocus => true
      = f.input :password, :required => false
      = f.input :remember_me, :as => :boolean, input_html: {class: 'flush--top'} if devise_mapping.rememberable?

    .form-actions
      = render "pro/admins/shared/links"
      %br
      = f.button :submit, "Se connecter", class: 'btn__success'

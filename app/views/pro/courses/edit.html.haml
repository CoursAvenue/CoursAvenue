= side_menu_currently_at 'Mes cours'

%ol.nav.breadcrumb.alpha.header-group
  %li= link_to 'Tous mes cours', pro_structure_path(@structure)
  %li= @course.name

%ul.tab-titles{data: {behavior: 'tabs'}}
  %li.tab-el.active
    %i.lbc-icon-info i
    Info
  %li
    = link_to pro_course_prices_path(@course), class: 'tab-el' do
      € Tarif
  %li
    = link_to pro_course_plannings_path(@course), class: 'tab-el' do
      %i.icon-calendar
      Planning

= render partial: 'form'

= side_menu_currently_at 'Mes cours'

%h3.header-group Édition de #{@course.name}
%p
  = link_to pro_structure_path(@structure) do
    %i.icon-double-angle-left
    Retour aux cours
  &nbsp;|&nbsp;
  = link_to '#' do
    %b
      %i.lbc-icon-info i
      Info
  &nbsp;|&nbsp;
  = link_to pro_course_prices_path(@course) do
    € Tarif
  &nbsp;|&nbsp;
  = link_to pro_course_plannings_path(@course) do
    %i.icon-calendar
    Planning

= render partial: 'form'

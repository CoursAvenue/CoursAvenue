= side_menu_currently_at 'pro_dashboard'

.panel
  = render partial: 'pro/shared/dashboard_tabs', locals: { current: 'flyers' }
  .panel__body
    %table.table--data.table--striped
      %thead
        %tr
          %th Date
          %th.one-half Image
          %th Traité
      %tbody
      - @flyers.each do |flyer|
        %tr
          %td= I18n.l(flyer.created_at, format: :date)
          %td= link_to 'Image', pro_flyer_path(flyer), data: { behavior: 'modal', width: '800', padding: 0 }
          %td.nowrap
            %input{ type: 'checkbox' }

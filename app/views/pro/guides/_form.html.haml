- form_url = @guide.persisted? ? pro_guide_path(@guide) : pro_guides_path

= simple_form_for @guide, url: form_url do |f|
  = f.input :title, label: 'Titre', label_html: { class: 'label--large' }, input_html: { class: 'input--large', placeholder: 'Quelle activité pour mon enfant ?' }
  = f.input :description, label: 'Description', label_html: { class: 'label--large' }, input_html: { placeholder: "L'année scolaire est à peine terminée et tous les parents sont déjà obligés de se poser cette question cruciale : quelle discipline sportive, culturelle ou créative proposer à son enfant à la rentrée ?" }
  = f.input :call_to_action, label: 'Accroche', label_html: { class: 'label--large' }, input_html: { placeholder: "En quelques questions, laissez-nous trouver des idées d'activités qui conviennent parfaitement aux goûts et aux besoins de votre enfant :" }

  %hr

  %div{ data: { behavior: 'show-more-on-demand' } }
    = f.simple_fields_for :questions, @guide.questions do |qf|
      - question = qf.object
      %div{ class: (question.persisted? ? '': 'hidden'), data: { el: true, hidden: (!question.persisted? and qf.index > 0) } }
        = qf.input :content, label: 'Question', label_html: { class: 'label--large' }, input_html: { class: 'input--large', placeholder: "Quel type d'activité?" }
        = qf.input :ponderation, label: 'Ponderation', input_html: { placeholder: '0' }

        %a.cursor-pointer.v-middle{ href: 'javascript:void(0);', data: { toggle: true } }
          %i.fa-eye
          Réponses
        .bordered.bordered--dash.bordered--larger.f-weight-500.mega-soft--ends{ class: (question.answers.map(&:content).any? ? '' : 'hidden'), data: { content: true } }
          = qf.simple_fields_for :answers, question.answers do |af|
            = af.input :content, label: 'Reponse', label_html: { class: 'label--large' }, input_html: { class: 'input--large', placeholder: "Physique" }
            = af.input :subject_ids, as: :select, collection: Subject.order('position ASC'), label_method: :name,
              input_html: { data: { behavior: 'chosen', placeholder: 'Sélectionnez une ou plusieurs disciplines', autocomplete: "false" }, multiple: true }

        .text--right
          %a.cursor-pointer.v-middle{ href: 'javascript:void(0);', data: { clear: true } }
            %i.fa-trash-o
            Supprimer la question
        %hr.push-half--ends

    .push--ends
      %a{ href: 'javascript:void(0)', data: { trigger: true } }>
        %i.fa.fa-plus
        Ajouter une question

  = f.submit 'Enregistrer', class: 'btn btn--green btn--full', data: { disable_with: "En cours d'enregistrement"}

- content_for :scripts do
  :javascript
    $(document).ready(function() {
        $('[data-toggle]').click(function(event) {
            event.preventDefault();
            $(this).siblings('[data-content]').slideToggle()
        });

    //  $('#guide_description').redactor({
    //      buttons: ['formatting', 'bold', 'italic', 'unorderedlist', 'orderedlist',
    //        'link', 'alignment', 'horizontalrule', 'image'],
    //      formatting: ['p', 'blockquote', 'h1', 'h2', 'h3'],
    //      lang: 'fr'
    //  });
    });

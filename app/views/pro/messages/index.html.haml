= side_menu_currently_at 'Derniers messages'

- @messages.each do |message|
  %div.text--right
    À
    - recipient = message.recipients.reject{|recipient| recipient == message.sender }[0]
    - if recipient.is_a? Admin
      = link_to recipient.name, pro_structure_path(recipient.structure)
    - else
      = recipient.name
  = render partial: 'messages/message', locals: { message: message }

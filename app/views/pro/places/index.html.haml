= side_menu_currently_at "Lieux d'enseignements"

%h1.text--center Renseignement des lieux d'enseignements — Étape 3/4

%table.table--striped
  %thead
    %tr
      %th Nom du lieu
      %th Ville
      %th.text--center Nombre de salles
      %th
      %th
  %tbody
    - @places.each do |place|
      %tr
        %td= link_to place.name, edit_structure_place_path(@structure, place)
        %td= place.city.name
        %td.text--center= place.rooms.count
        %td
          - if place.rooms.count == 0
            = link_to "J'ai plusieurs salles : je les renseigne ici !", structure_place_rooms_path(@structure, place), class: 'btn'
          - else
            = link_to 'Gérer mes salles', structure_place_rooms_path(@structure, place)
        %td
          = link_to edit_structure_place_path(@structure, place), title: 'Éditer', class: 'btn btn__warning' do
            %i.icon-edit
          = link_to structure_place_path(@structure, place), method: :delete, data: {confirm: 'Êtes-vous sur de supprimer ce lieu ? Cette action sera irreversible.'}, title: 'Supprimer', class: 'btn btn__danger' do
            %i.icon-trash
= link_to 'Ajouter un nouveau lieu', new_structure_place_path(@structure), class: 'btn btn__primary'

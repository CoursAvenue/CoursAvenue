= side_menu_currently_at 'Mes cours'

%h2.header-group.flush--bottom Planning - #{@course.name}
%p
  = link_to pro_structure_path(@structure) do
    %i.icon-double-angle-left
    Retour aux cours
  &nbsp;|&nbsp;
  = link_to edit_pro_course_path(@course) do
    %i.lbc-icon-info i
    Info
  &nbsp;|&nbsp;
  = link_to pro_course_prices_path(@course) do
    € Tarif
  &nbsp;|&nbsp;
  = link_to '#' do
    %b
      %i.icon-calendar
      Planning

- if @plannings.any?
  %h3 Planning de mon cours
  = render "#{@course.underscore_name}_list"
  %hr

%h5.form-header-group.flush--bottom Rajouter un créneau
.form-white-box
  = render "#{@course.underscore_name}_form"
/ %p S'il s'agit d'un cours particulier, ajoutez autant de créneaux que de séances.

= content_for :scripts do
  :javascript
    window.addEvent('domready', function() {
        $('add-teacher').addEvent('click', function(event) {
            $('add-teacher-wrapper').dispose()
            $('new-teacher-wrapper').show();
        });
    });

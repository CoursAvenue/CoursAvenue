= side_menu_currently_at "Lieux d'enseignements"

%h3.header-group.flush--bottom Gestion des salles de : #{@place.name}
%div
  = link_to structure_places_path(@structure) do
    %i.icon-double-angle-left
    Retour aux lieux

- unless @rooms.empty?
  %table.table--striped
    %thead
      %tr
        %th Nom
        %th Superficie
        %th
    %tbody
      - @rooms.each do |room|
        - unless room.new_record?
          %tr
            %td= room.name
            %td= room.surface
            %td
              = link_to [@structure, @place, room], method: :delete, data: {confirm: 'Êtes-vous sur de supprimer cette salle ? Cette action sera irreversible.'} do
                %i.icon-trash

  %hr

%div.text--center
  %p.alert--info Si vous n'avez qu'une seule salle vous n'êtes pas obligé d'en créer.

%h4 Ajouter une salle
= render 'form'

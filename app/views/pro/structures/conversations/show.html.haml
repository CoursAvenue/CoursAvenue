= side_menu_currently_at 'Mes messages'

%h3 Mes messages
.text--center
  = link_to 'Nouveau message', new_pro_structure_message_path(@structure), class: 'btn btn--blue btn--large'

- @conversations.each do |conversation|
  = debug conversation


.panel
  .panel__header.soft
    .delta.line-height-normal
      Suppression du cours :
      %br
      %strong= @course.name
  .panel__body
    .soft
      %p
        Attention, la suppression aura pour effet de supprimer tous les créneaux liés à ce cours.
      .grid
        .grid__item.one-third>
          %a.btn.btn--full{ onclick: '$.fancybox.close()' } Annuler
        .grid__item.two-thirds>
          = link_to 'Supprimer ce cours', pro_structure_course_path(@structure, @course), class: 'red', method: :delete, class: 'btn btn--full btn--red', remote: true, data: { disable_with: 'Suppression en cours...' }

%table.table--striped.table--data{ id: "course-planning-list-#{course.id}"}
  %thead
    %tr
      %th Jour
      %th Créneau horaire
      %th Niveau
      %th Public
      %th Lieu
      %th
        %i.lbc-icon-info i
      %th
  %tbody
    - course.plannings.each do |planning|
      = render partial: 'pro/structures/courses/plannings/lesson_row', locals: { planning: planning, course: course }

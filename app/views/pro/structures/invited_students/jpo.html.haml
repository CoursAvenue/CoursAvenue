= side_menu_currently_at "Journées Portes Ouvertes"

= render partial: 'pro/structures/open_courses/tabs', locals: { current: 'invite-jpo-list' }

%h2 Invitations

%table.table--striped.table--data.white-box
  %thead
    %tr
      %th Email
      - if current_pro_admin.super_admin?
        %th
  %tbody
    - @invited_users.each do |invited_user|
      %tr
        %td= invited_user.email
        - if current_pro_admin.super_admin?
          %td= link_to 'Supprimer', pro_structure_invited_student_path(@structure, invited_user), method: :delete, class: 'red'

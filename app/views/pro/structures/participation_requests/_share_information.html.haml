- structure           = @structure || @participation_request.structure
- structure_decorator = structure.decorate
- user_decorator      = @participation_request.user.decorate

%p Une fois la demande acceptée, vos infos de contact seront échangées :
.flexbox.push--bottom
  .flexbox__item.one-half.v-middle>
    .flexbox
      .flexbox__item{ style: 'width: 50px;' }
        = image_tag structure.logo.url(:thumb), class: 'block center-block bordered rounded--circle push-half--bottom', width: 50, height: 50
      .flexbox__item.soft-half--left
        %div
          %strong= truncate(structure.name, length: 20)
        = truncate(structure_decorator.phone_numbers((on_pro_subdomain? ? false : true), false), length: 20)
        - if on_pro_subdomain? and structure.website.present?
          %div= structure_decorator.website_link(20)
  .flexbox__item.text--center.v-middle.soft-half>
    %i.fa-right-to-left.fa-3x
  .flexbox__item.one-half.v-middle>
    .flexbox
      .flexbox__item{ style: 'width: 50px;' }
        = image_tag @participation_request.user.avatar_url(:thumb), class: 'block center-block bordered rounded--circle push-half--bottom', width: 50, height: 50
      .flexbox__item.soft-half--left
        %div
          %strong= truncate(user_decorator.full_name, length: 20)
        %div= user_decorator.phone_number_and_email((on_pro_subdomain? ? true : false))

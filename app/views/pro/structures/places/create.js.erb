<% if @place.persisted? %>
    $.fancybox("<%= escape_javascript(render partial: 'pro/structures/places/verify_address') %>", { width: 600, minWidth: 600 });
<% else %>
    $.fancybox("<%= escape_javascript(render partial: 'pro/structures/places/form') %>", { width: 900, minWidth: 900 });
<% end %>
$.each(GLOBAL.initialize_callbacks, function(i, func) { func(); });

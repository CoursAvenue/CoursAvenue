<% if @place.is_home? %>
    $('#update-home-place').hide();
    $('#new-home-place').show();
<% end %>
$('#place-<%= @place.id %>').remove();
$.fancybox.close();
window.coursavenue.redrawMap(<%== @place_coordinates.to_json %>);

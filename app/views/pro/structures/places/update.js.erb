<% if @place.errors.empty? %>
    $.fancybox("<%== escape_javascript(render partial: 'pro/structures/places/verify_address') %>", { width: 600, minWidth: 600, padding: 0, helpers: { overlay : { closeClick: false } } });
<% else %>
    $.fancybox("<%== escape_javascript(render partial: 'pro/structures/places/form') %>", { width: 900, minWidth: 900, padding: 0, helpers: { overlay : { closeClick: false } } });
<% end %>
$.each(GLOBAL.initialize_callbacks, function(i, func) { func(); });

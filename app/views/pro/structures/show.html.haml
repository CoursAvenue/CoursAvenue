= side_menu_currently_at 'Mes infos'
= render partial: 'pro/structures/tabs', locals:{ current: 'établissement'}

= link_to edit_pro_structure_path(@structure), class: 'btn btn--orange btn--small push--bottom' do
  %i.icon-pencil
  %span.soft-half--left Modifier mes informations

= render 'pro/structures/structure'

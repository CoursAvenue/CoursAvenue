%h1 Votre abonnement

%h3 État de votre abonnement
- if @subscription.canceled?
  = render 'subscription_canceled'
- elsif @subscription.in_trial? and @subscription.stripe_subscription_id.nil?
  = render 'subscription_in_trial'
- elsif @subscription.stripe_subscription_id
  = render 'subscription_running'
- else
  = render 'subscription_trial_ended'

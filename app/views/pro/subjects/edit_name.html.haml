%h3
  - if @subject.parent
    = "#{@subject.root.name} / #{@subject.parent.name}"
  - else
    = @subject.root.name
= simple_form_for [:pro, @subject] do |f|
  = f.input :name
  = f.submit 'Enregistrer', class: 'btn btn--full btn--green', disable_with: "En train d'enregistrer"


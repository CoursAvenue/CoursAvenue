.soft--sides.soft--top
  - @coupons.each do |coupon|
    .flexbox.push--bottom
      .flexbox__item.v-middle
        = link_to pro_subscriptions_coupon_path(coupon), class: 'fancybox.ajax', data: { behavior: 'modal', width: '700', padding: '0' } do
          %h4.flush--bottom
            = coupon.name
            %span.caps.bg-green.white.smaller-print active

        = link_to 'Voir sur Stripe', "https://dashboard.stripe.com/test/coupons/#{coupon.stripe_coupon_id}", target: :_blank

      .flexbox__item.v-middle.text--right
        = succeed ' ' do
          = link_to pro_subscriptions_coupon_path(coupon), class: 'btn btn--bigger fancybox.ajax', data: { behavior: 'modal', width: '700', padding: '0' } do
            %i.fa.fa-th-list
        = succeed ' ' do
          = link_to pro_subscriptions_coupon_path(coupon), class: 'btn btn--bigger btn--red', method: :delete, data: { confirm: 'Êtes sûr de vouloir de supprimer cette offre ? Elle sera aussi supprimée de Stripe.' } do
            %i.fa-trash-o

    %hr

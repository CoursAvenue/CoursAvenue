- params[:week_days]   ||= [] # I18n.t('date.day_names')
- params[:time_slots]  ||= [] # CoursAvenue::Application::TIME_SLOTS
- params[:time_range]  ||= {}

.home-page-search
  = form_tag structures_path, method: :get, authenticity_token: false, class: 'header-search-bar--dark', id: 'header-search' do
    = render 'shared/structures_header_search'

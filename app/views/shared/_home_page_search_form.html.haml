- params[:week_days]   ||= [] # LeBonCours::Application::WEEK_DAYS
- params[:time_slots]  ||= [] # LeBonCours::Application::TIME_SLOTS
- params[:time_range]  ||= {}

.home-page-search
  = form_tag course_groups_path, method: :get, authenticity_token: false, class: 'header-search-bar--dark' do
    = render 'shared/header_search'

.p Cher fondateurs,

.p
  #{@structure.name} (#{@admin.email}) vient de s'inscrire !
  %br
  Disciplines : #{@structure.subjects.map(&:name).join(', ')}

.p.action
  = link_to 'Voir son profil', dashboard_pro_structure_url(@structure, subdomain: 'pro'), class: 'btn btn--green'

= render 'shared/mailers/footer'

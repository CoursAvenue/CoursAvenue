= user_menu_currently_at 'Inviter mes amis'

= render partial: 'users/invited_users/tabs', locals: { current: 'new' }

%h2.text--center Invitez vos amis sur CoursAvenue

.white-box.soft
  = form_tag(bulk_create_user_invited_users_path(current_user), method: :post) do
    .grid
      .grid__item.one-half>
        %h4 À :
        %p.flush
          %i Entrez les emails des amis que vous souhaitez inviter.
        %textarea.one-whole{name: 'emails', rows: 8, placeholder: 'Ex. : patrick@gmail.com, thomas@wanadoo.fr', data: {behavior: 'autoresize'}}
      .grid__item.one-half>
        %h4 Message :
        %br
        %textarea.one-whole{name: 'text', rows: 8, data: {behavior: 'autoresize'}}
          Salut !&#x000A;&#x000A;Je viens de me créer un compte sur CoursAvenue et c'est trop cooooool ! C'est gratuit, simple et rapide.

        %p.gray-light.flush
          A+ !
          %br
          = current_user.name

    .text--right.push-half--top= submit_tag 'Envoyer les invitations', class: 'btn btn--green'

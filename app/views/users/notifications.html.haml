= user_menu_currently_at 'infos'

.panel
  = render partial: 'tabs', locals: { current: 'notifications' }
  .panel__body.soft
    %h4 Mes notifications e-mail

    = simple_form_for @user do |f|
      %input{type: 'hidden', name: 'return_to', value: notifications_user_path(current_user)}
      .grid
        .grid__item.one-third>
          %strong Envoyez-moi des e-mails quand :
          %p.blue
            %i Nous vous promettons de ne pas vous envoyer de spam.
        .grid__item.two-thirds.check_boxes>
          .checkbox-at-left
            .push--bottom= f.input :email_promo_opt_in, wrapper_html: { class: 'flush' }
            .push--bottom= f.input :email_newsletter_opt_in, wrapper_html: { class: 'flush' }
            .push--bottom= f.input :email_passions_opt_in, wrapper_html: { class: 'flush' }

      = f.submit 'Enregistrer', class: 'btn btn--green btn--full', data: { disable_with: "En cours d'enregistrement" }


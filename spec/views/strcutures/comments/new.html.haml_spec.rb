require "rails_helper"

describe "structures/comments/new.html.haml" do
  let(:structure) { FactoryGirl.create(:structure) }

  # it "works" do
  #   assign(:structure, structure)
  #   subject   = mock_model(Subject, name: "whaterver", id: 4)
  #   structure = mock_model(Structure, name: "whaterver", id: 4, subject_ids: [subject.id])
  #   assign(:comment, Comment.new)
  #   render
  # end
end
